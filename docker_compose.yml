version: "3.9"
services:

  aabrham:
    build: .
    depends_on:
      - redis
    ports:
      - "18080:18080"

  redis:
    image: "redis/redis-stack"
    ports:
      - "6379:6379"
      - "8001:8001"

# docker compose va usato solo per rilasciare